apiVersion: v1
kind: Service
metadata:
  name: resume-app
  namespace: resume-app
spec:
  type: LoadBalancer
  selector:
    app: resume-app
  ports:
  - port: 80
    targetPort: 80
    name: http
  - port: 443
    targetPort: 443
    name: https
